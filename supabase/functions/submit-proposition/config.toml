# Configuration for submit-proposition Edge Function
#
# verify_jwt = false: Function validates auth internally (validateAuth).
# Supports both user JWTs and service role keys (for agent-propose calls).
# Gateway JWT check blocked service_role tokens, breaking agent flow.

[function]
verify_jwt = false
